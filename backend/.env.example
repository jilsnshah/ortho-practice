# Copy to backend/.env. All settings use the ORTHO_ prefix.
ORTHO_SECRET_KEY=replace-with-output-of: python -c "import secrets; print(secrets.token_urlsafe(48))"
# ORTHO_DATABASE_URL=postgresql+psycopg://user:pass@host/ortho
# ORTHO_UPLOAD_DIR=/var/lib/ortho/uploads
# ORTHO_COOKIE_SECURE=true
